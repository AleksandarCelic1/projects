package app;

public class DatabaseManager
{
  /*
    In java you can't do private: declare 10 vars and then public: declare 10 vars, you must
    type private/public for each var <!>
  */

    public DatabaseManager()
    {
        /*
            Open the env file and parse the DB credentials in order to log in <!>
         */
    }


  /*
    Should be the one that interacts with the postgreSQL server
    Implement a pool of SQL connections so we can dispatch multiple SQL queries concurrently
   */
}