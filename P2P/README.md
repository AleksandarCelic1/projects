Problems:
1. We have a hard-coded port -> is this really a problem ? 
2. If we were to not have a hard-coded port, we would've to find a way for other peers to be able to discover it 
3. Security is at its finest, meaning we have NO encryption at all, will try to figure out a way to avoid MITM attack

